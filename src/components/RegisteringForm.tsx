import React, { useEffect } from "react";
import { useForm } from "react-hook-form";
import { SnailInterface } from "../App";

function RegisteringForm({ caracois, onSetCaracois }) {
 
}

export default RegisteringForm;
